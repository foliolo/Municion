package al.ahgitdevelopment.municion.ui.theme

import androidx.compose.ui.graphics.Color

// Colores del proyecto actual (colors.xml)
val Primary = Color(0xFF13747D)           // colorPrimary - teal/verde azulado
val PrimaryDark = Color(0xFF29221F)       // colorPrimaryDark - marrón oscuro
val Secondary = Color(0xFFFC354C)         // colorAccent - rojo coral
val Background = Color(0xFFFAFAFA)        // Fondo claro
val Surface = Color(0xFFFFFFFF)           // Superficie blanca
val Error = Color(0xFFB00020)             // Error rojo Material
val OnPrimary = Color(0xFFFFFFFF)         // Texto sobre primary
val OnSecondary = Color(0xFFFFFFFF)       // Texto sobre secondary
val OnBackground = Color(0xFF1C1C1C)      // Texto sobre background
val OnSurface = Color(0xFF1C1C1C)         // Texto sobre surface
val OnError = Color(0xFFFFFFFF)           // Texto sobre error

// Colores adicionales para la UI
val BottomNavBackground = PrimaryDark
val BottomNavItemActive = Color(0xFFFFFFFF)
val BottomNavItemInactive = Color(0x99FFFFFF)
val Selected = Color(0xFFACB3B4)
val LightYellow = Color(0xFFFAFAD2)

// Colores para estados de licencias
val LicenseValid = Color(0xFF4CAF50)      // Verde - válida
val LicenseExpiring = Color(0xFFFFC107)   // Amarillo - próxima a caducar
val LicenseExpired = Color(0xFFF44336)    // Rojo - caducada

// Color terciario para tiradas
val Tertiary = Color(0xFFFF9800)          // Naranja - tiradas/deportes
